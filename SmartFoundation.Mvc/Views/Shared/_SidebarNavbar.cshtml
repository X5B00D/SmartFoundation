@using System.Text.Json
@using Microsoft.AspNetCore.Html
@using System.Linq
@using System.Text
@using System.Text.Encodings.Web
@using SmartFoundation.Mvc.Models
@model List<MenuItem>




@{
    // غيّر "userID" إذا اسم السيشن عندك مختلف
    var sessionUserId = Context.Session.GetString("userID");
    var sessionfullname = Context.Session.GetString("fullName");
    var sessionUserPhoto = Context.Session.GetString("photoBase64");
}

<nav class="fixed z-50 w-full border-bottom" style="background:#1f2937; color:#e2e8f0; border-color:#334155">
    <div class="px-3 py-3 lg:px-5 lg:pe-3">
        <div class="flex items-center justify-between">
            <div class="flex items-center justify-start">
                <!-- زر إظهار السايدبار -->
                <button data-drawer-target="logo-sidebar" data-drawer-toggle="logo-sidebar" aria-controls="logo-sidebar"
                        type="button"
                        class="inline-flex items-center p-2 rounded-lg text-slate-300 lg:hidden hover:text-slate-50 hover:bg-slate-700 focus:bg-slate-700 focus:ring-2 focus:ring-slate-600">
                    <span class="sr-only">Open sidebar</span>
                    <svg id="toggleSidebarMobileHamburger" class="w-6 h-6" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd"
                              d="M3 5a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zM3 10a1 1 0 011-1h6a1 1 0 110 2H4a1 1 0 01-1-1zM3 15a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1z"
                              clip-rule="evenodd"></path>
                    </svg>
                </button>

                <!-- الشعار -->
                <a href="/" class="flex ms-1 md:me-10">
                    <img src="../img/ppng.png" class="h-12 me-2" alt="KFMC Logo" />
                    <span class="self-center font-semibold text-lg sm:text-1xl whitespace-nowrap text-slate-100">البوابة الموحدة</span>
                </a>

                <!-- البحث -->
                <form action="#" method="GET" class="block lg:ps-3.5">
                    <label for="topbar-search" class="sr-only">بحث</label>
                    <div class="relative mt-1 lg:w-96" dir="rtl">
                        <div class="absolute inset-y-0 left-0 flex items-center pointer-events-none ps-3">
                            <svg class="w-10 h-5 text-slate-300" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd"
                                      d="M8 4a4 4 0 100 8 4 4 0 000-8zM2 8a6 6 0 1110.89 3.476l4.817 4.817a1 1 0 01-1.414 1.414l-4.816-4.816A6 6 0 012 8z"
                                      clip-rule="evenodd"></path>
                            </svg>
                        </div>
                        <input id="topbar-search" type="text"
                               class="block w-full rounded-md border border-slate-600 bg-slate-700 p-2.5 ps-10 text-sm text-slate-100 placeholder-slate-300 focus:border-slate-500 focus:ring-slate-500"
                               placeholder="بحث..">
                    </div>
                </form>
            </div>

            <!-- جهة اليمين -->
            <div class="flex items-center gap-0">
                <!-- روابط سريعة -->
                <nav class="flex items-center gap-0.5 me-4 flex-none" dir="rtl" aria-label="روابط سريعة">
                    <a href="/Home" class="inline-flex items-center gap-1.5 px-3 py-1.5 text-sm text-slate-200 rounded-md hover:bg-slate-700">
                        <i class="fa-solid fa-house text-[16px]"></i>
                        <span>الصفحة الرئيسية</span>
                    </a>
                    <a href="/Account/ChangePassword" class="inline-flex items-center gap-1.5 px-3 py-1.5 text-sm text-slate-200 rounded-md hover:bg-slate-700">
                        <i class="fa-solid fa-key text-[16px]"></i>
                        <span>تغيير كلمة المرور</span>
                    </a>
                </nav>

                <!-- الإشعارات -->
                <button type="button"
                        data-dropdown-toggle="notification-dropdown"
                        class="relative p-1 rounded-lg text-slate-300 hover:text-slate-50 hover:bg-slate-700"
                        aria-haspopup="true" aria-expanded="false">


                    <span class="sr-only">عرض الإشعارات</span>

                    <!-- أيقونة الجرس -->
                    <svg class="w-6 h-7" fill="currentColor" viewBox="0 0 20 20">
                        <path d="M10 2a6 6 0 00-6 6v3.586l-.707.707A1 1 0 004 14h12a1 1 0 00.707-1.707L16 11.586V8a6 6 0 00-6-6zM10 18a3 3 0 01-3-3h6a3 3 0 01-3 3z"></path>
                    </svg>

                    <!-- Badge عدد الإشعارات -->
                    <span id="notif-count"
                          class="absolute -top-0.5 -right-0.5 
             min-w-[15px] h-[17px]
             px-1.5 rounded-full
             bg-red-600 text-white text-[10px]
             flex items-center justify-center
             font-bold ring-1 ring-slate-800 hidden">

                    </span>

                </button>


                <div id="notification-dropdown" class="z-50 hidden my-2 overflow-hidden rounded-lg shadow-lg w-80 bg-slate-800 ring-1 ring-slate-700">
                    <div class="px-4 py-2 text-base font-medium text-center text-slate-200">الإشعارات</div>
                    <div>
                        <a href="/Housing/Index" class="flex px-4 py-3 border-t border-slate-700 hover:bg-slate-700/60">
                            <div class="w-full">
                                <div class="mb-1.5 text-sm text-slate-200">رسالة جديدة من المستخدم</div>
                                <div class="text-xs font-medium text-emerald-400">قبل دقائق</div>
                            </div>
                        </a>
                    </div>
                    <a href="#" class="block py-2 text-base text-center bg-slate-700 text-slate-100 hover:bg-slate-600">عرض الكل</a>
                </div>

                <!-- المستخدم -->
                <div class="flex items-center ms-3">
                    <div>
                        <button type="button"
                                class="p-3 rounded-lg text-slate-300 hover:text-slate-50 hover:bg-slate-700 hover:shadow-md transition
                                focus:ring-1 focus:ring-slate-400"
                                id="user-menu-button" aria-expanded="false" data-dropdown-toggle="user-dropdown">

                            <span class="sr-only">فتح قائمة المستخدم</span>

                            @if (!string.IsNullOrEmpty(sessionUserPhoto))
                            {
                                <img class="w-8 h-8 rounded-full"
                                     src="data:image/png;base64,@sessionUserPhoto"
                                     alt="user photo">
                            }
                            else
                            {
                                <img class="w-8 h-8 rounded-full"
                                     src="~/img/user_.png"
                                     alt="user photo">
                            }
                            @* <img class="w-8 h-8" src="~/img/user_.png" alt="user photo"> *@
                            <i class="fa-solid fa-gear text-[16px] text-slate-300"></i>
                        </button>
                    </div>

                    <!-- User dropdown -->
                    <div id="user-dropdown"
                         class="z-50 hidden w-55 my-2 overflow-hidden rounded-sm shadow-sm bg-slate-800 ring-1 ring-slate-700">
                        <div class="px-4 py-3">
                            <p class="text-sm font-medium text-slate-200">@(sessionfullname?.ToString() ?? "غير معروف")</p>
                            <p class="mt-0.5 text-xs text-slate-400 truncate">@(sessionUserId?.ToString() ?? "غير معروف")</p>
                            <p class="text-sm font-medium text-slate-200">@(sessionfullname?.ToString() ?? "غير معروف")</p>
                            <p class="mt-0.5 text-xs text-slate-400 truncate">@(sessionUserId?.ToString() ?? "غير معروف")</p>
                        </div>
                        <ul class="py-1">
                            <li>
                                <hr class="my-1 border-slate-700">
                                <a href="/Dashboard" class="flex items-center gap-2 px-4 py-2 text-sm text-slate-200 hover:bg-slate-700">
                                    <i class="fa-solid fa-gauge text-[13px] text-slate-300"></i> لوحة التحكم
                                </a>
                            </li>
                            <li>
                                <hr class="my-1 border-slate-700">
                                <a href="/Settings" class="flex items-center gap-2 px-4 py-2 text-sm text-slate-200 hover:bg-slate-700">
                                    <i class="fa-solid fa-gear text-[13px] text-slate-300"></i> الإعدادات
                                </a>
                            </li>
                            <li><hr class="my-1 border-slate-700"></li>
                            <li>
                                <a href="/Login/Logout" class="flex items-center gap-2 px-4 py-2 text-sm text-red-400 hover:bg-slate-700">
                                    <i class="fa-solid fa-right-from-bracket text-[13px]"></i> تسجيل الخروج
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>

        </div>
    </div>
</nav>


<aside id="logo-sidebar"
       class="fixed top-0 right-0 z-40 w-64 h-screen translate-x-full border-l pt-25 sm:translate-x-0 bg-slate-800 text-slate-200 border-slate-700"
       aria-label="Sidebar">


    <div class="relative flex flex-col flex-1 h-full min-h-0 pt-0 bg-transparent">
        <div class="flex flex-col flex-1 pt-5 pb-4 overflow-y-auto"
             style="scrollbar-width: none; -ms-overflow-style: none;">
            <style>
                /* إخفاء شريط التمرير */
                #logo-sidebar .overflow-y-auto::-webkit-scrollbar {
                    display: none;
                }

                /* خلفية وألوان */
                #logo-sidebar {
                    background: #1f2937;
                    border-top: 0 !important;
                }

                    #logo-sidebar .bg-white {
                        background: transparent !important;
                    }

                    #logo-sidebar .text-gray-900 {
                        color: #e2e8f0 !important;
                    }

                    #logo-sidebar .divide-y > :not([hidden]) ~ :not([hidden]) {
                        border-color: #334155 !important;
                    }

                    /* روابط وأزرار */
                    #logo-sidebar a,
                    #logo-sidebar button {
                        color: #f8fafc;
                        display: flex;
                        align-items: center;
                        gap: 0rem;
                        white-space: normal !important; 
                        word-break: break-word !important; 
                        overflow: visible !important;
                        text-overflow: unset !important;
                        line-height: 1.25; 
                        font-size: 15px; 
                        padding-top: 0.60rem !important;
                        padding-bottom: 0.45rem !important;
                    }

                        /*  hover */
                        #logo-sidebar a:hover,
                        #logo-sidebar button:hover,
                        #logo-sidebar .hover\:bg-white:hover {
                            background: #334155 !important;
                            color: #f8fafc !important;
                        }

                    
                    #logo-sidebar ul > li > ul {
                        padding-right: 0.75rem;
                        margin-top: 0.10rem;
                        border-right: none;
                    }

                        /* إلغاء الخط العمودي القديم */
                        #logo-sidebar ul > li > ul::before {
                            content: none;
                            border-right: none;
                        }

                    /* إلغاء أي نقاط قديمة */
                    #logo-sidebar ul li ul li a::after,
                    #logo-sidebar ul li ul li button::after {
                        content: none;
                    }

                    /* العناصر الفرعية (المستوى الثاني) */
                    #logo-sidebar ul > li > ul > li > a,
                    #logo-sidebar ul > li > ul > li > button {
                        position: relative;
                        padding-top: 0.40rem !important;
                        padding-bottom: 0.40rem !important;
                        padding-right: 0.75rem !important;
                        padding-left: 0.75rem !important;
                        margin-inline: 0.25rem;
                        border-radius: 0.3rem;
                        font-size: 14px;
                        line-height: 1.4;
                        opacity: 0.9;                       
                        background: transparent;
                        box-shadow: 0 0 0 0 rgba(15, 23, 42, 0);
                        transform: translateX(0);
                        transition: background-color 0.18s ease, color 0.18s ease, box-shadow 0.18s ease, transform 0.14s ease, opacity 0.18s ease;
                    }

                        /* hover */
                        #logo-sidebar ul > li > ul > li > a:hover,
                        #logo-sidebar ul > li > ul > li > button:hover {
                            background: #334155 !important; 
                            color: #f9fafb !important;
                            opacity: 1;
                            transform: translateX(-4px); 
                            box-shadow: 0 4px 10px rgba(15, 23, 42, 0.45);
                        }

                        
                        #logo-sidebar ul > li > ul > li > a.bg-slate-700,
                        #logo-sidebar ul > li > ul > li > button.bg-slate-700 {
                            background: #334155 !important;
                            color: #f9fafb !important;
                            box-shadow: 0 0 0 1px #1e293b, 0 6px 14px rgba(15, 23, 42, 0.55);
                            opacity: 1;
                            transform: translateX(-2px);
                        }

                        /* مؤشر على يمين العنصر    */
                        #logo-sidebar ul > li > ul > li > a::before,
                        #logo-sidebar ul > li > ul > li > button::before {
                            content: "";
                            position: absolute;
                            right: 0.15rem;
                            top: 50%;
                            transform: translateY(-50%);
                            width: 3px;
                            height: 10px; 
                            border-radius: 999px;
                            background: transparent;
                            transition: background 0.18s ease, height 0.18s ease, transform 0.14s ease;
                        }

                        #logo-sidebar ul > li > ul > li > a:hover::before,
                        #logo-sidebar ul > li > ul > li > button:hover::before,
                        #logo-sidebar ul > li > ul > li > a.bg-slate-700::before,
                        #logo-sidebar ul > li > ul > li > button.bg-slate-700::before {
                            background: #15803d; 
                            height: 18px; 
                            transform: translateY(-50%) scaleX(1.02);
                        }

                        
                        #logo-sidebar ul > li > ul > li > a:focus-visible,
                        #logo-sidebar ul > li > ul > li > button:focus-visible {
                            outline: none;
                            box-shadow: 0 0 0 2px rgba(21, 128, 61, 0.75);
                            transform: translateX(-2px);
                            opacity: 1;
                        }

            </style>


            @* <div class="flex-1 px-2.5 space-y-1 divide-y">
                <ul class="pb-2 space-y-2">
                    <li id="menu-container"></li>
                </ul>
            </div> *@


            <div class="flex-1 px-4 space-y-1">

                <!-- بطاقة المستخدم -->
                <div class="py-1 flex flex-col items-center text-center">

                    @{
                        var photo = sessionUserPhoto;
                        var hasPhoto = !string.IsNullOrWhiteSpace(photo);
                    }

                    <div class="w-16 h-16 rounded-full overflow-hidden bg-slate-700/60
                    flex items-center justify-center mb-3 shadow-md ring-1 ring-slate-600">
                        @if (hasPhoto)
                        {
                            <img src="data:image/jpeg;base64,@photo"
                                 alt="user"
                                 class="w-full h-full object-cover" />
                        }
                        else
                        {
                            <i class="fa-regular fa-user text-2xl text-slate-300"></i>
                        }
                    </div>

                    <div class="text-sm font-medium text-slate-200 leading-tight">
                        @(sessionfullname?.ToString() ?? "غير معروف")
                    </div>

                    <div class="text-base font-bold text-emerald-400 mt-0.5">
                        @(sessionUserId?.ToString() ?? "غير معروف")
                    </div>
                </div>

                <!-- قائمة المنيو -->
                <ul class="pt-2 pb-2 space-y-2" style="margin-top:-6px;">
                    <li id="menu-container"></li>
                </ul>
            </div>




        </div>
    </div>
</aside>




<script>
        // ================================================================
        // 🔹 Multi-Level Menu Renderer for Sidebar
        // ================================================================

        const menuData = @Html.Raw(JsonSerializer.Serialize(
                                                Model ?? new List<MenuItem>(),
                                                new JsonSerializerOptions { Encoder = JavaScriptEncoder.UnsafeRelaxedJsonEscaping }
                                ));

        console.log("Menu data:", menuData);

        const currentPath = window.location.pathname.toLowerCase();

        function cleanMenuLink(link) {
            if (!link || typeof link !== "string") return "";
            let cleaned = link.trim();

            // ../Employee -> Employee
            while (cleaned.startsWith("../")) {
                cleaned = cleaned.substring(3);
            }
            // /Employee -> Employee
            if (cleaned.startsWith("/")) {
                cleaned = cleaned.substring(1);
            }
            // /BuildingClass.aspx -> /BuildingClass
            if (cleaned.toLowerCase().endsWith(".aspx")) {
                cleaned = cleaned.substring(0, cleaned.length - 5);
            }

            return cleaned;
        }

        function escapeHtml(text) {
            const div = document.createElement("div");
            div.textContent = text ?? "";
            return div.innerHTML;
        }






            // ترجع true إذا كان تحت هذا العنصر أي ابن (أو ابن ابن...) صفحته الحالية نشطة
    function hasActiveChildRecursive(item) {
        if (!Array.isArray(item.Children)) return false;

        for (const child of item.Children) {
            const rawController = child.MPLink ?? child.mpLink ?? "";
            const rawAction    = child.MenuLink ?? child.menuLink ?? "";

            const controller = cleanMenuLink(rawController);
            const action     = cleanMenuLink(rawAction);

            const childHasChildren = Array.isArray(child.Children) && child.Children.length > 0;
            const hasAnyLinkInfo   = (controller !== "" || action !== "");
            const isClickableLink  = hasAnyLinkInfo && !childHasChildren;

            let itemLink = "#";
            if (isClickableLink) {
                if (controller && action) {
                    itemLink = `/${controller}/${action}`;
                } else if (action) {
                    itemLink = `/${action}`;
                } else if (controller) {
                    itemLink = `/${controller}`;
                }
            }

            const isActive = isClickableLink && currentPath === itemLink.toLowerCase();

            if (isActive || hasActiveChildRecursive(child)) {
                return true;
            }
        }

        return false;
    }









        // ------------------------------------------------------------
        // 🔹 بناء عنصر واحد (recursive) بالاعتماد على Children من JSON
        // ------------------------------------------------------------
        function buildMenuItem(item, level) {
            const paddingClass = level > 1 ? " ps-10" : "";
            const baseClasses =
                "flex items-center w-full p-2 rounded-lg transition duration-75 group " +
                "hover:bg-slate-700 hover:text-slate-50 text-slate-200";
            const linkClasses = level === 1 ? baseClasses : baseClasses + paddingClass;

            const menuName = escapeHtml(item.MenuName_A || item.MenuNameForView || "");

            // ⭐ ندعم الأسماء الكبيرة/الصغيرة في JSON
            const rawController = item.MPLink ?? item.mpLink ?? "";
            const rawAction    = item.MenuLink ?? item.menuLink ?? "";

            const controller = cleanMenuLink(rawController);  // ../Employee -> Employee
            const action     = cleanMenuLink(rawAction);      // /BuildingClass.aspx -> BuildingClass

            const hasChildren     = Array.isArray(item.Children) && item.Children.length > 0;
            const hasAnyLinkInfo  = (controller !== "" || action !== "");
            const isClickableLink = hasAnyLinkInfo && !hasChildren;

            //  بناء الرابط النهائي
            let itemLink = "#";
            if (isClickableLink) {
                if (controller && action) {
                    itemLink = `/${controller}/${action}`;      // /Employee/BuildingClass
                } else if (action) {
                    itemLink = `/${action}`;
                } else if (controller) {
                    itemLink = `/${controller}`;
                }
            }

            const isActive = isClickableLink && currentPath === itemLink.toLowerCase();
            const activeClass = isActive ? " bg-slate-700 text-slate-50" : "";

            const iconHtml =
                item.MPIcon && item.MPIcon.trim() !== ""
                    ? `<i class="${escapeHtml(
                          item.MPIcon
                      )} w-5 h-5 me-3 text-slate-400 group-hover:text-slate-50 transition-colors duration-200"></i>`
                    : "";

            let html = "";

                if (hasChildren) {
        const collapseId = `dropdown-${item.MPID}`;

        
        const shouldBeOpen = hasActiveChildRecursive(item);
        const childListClasses = shouldBeOpen ? "py-2 space-y-2" : "hidden py-2 space-y-2";
        const expandedAttr = shouldBeOpen ? 'aria-expanded="true"' : 'aria-expanded="false"';

        html += `
        <button type="button"
                class="${linkClasses}${activeClass}"
                data-collapse-toggle="${collapseId}"
                aria-controls="${collapseId}"
                ${expandedAttr}>
          ${iconHtml}
        <span class="flex-1 text-right whitespace-normal break-words leading-snug">
            ${menuName}
        </span>
              <svg class="w-4 h-4 text-slate-400 group-hover:text-slate-50"
               fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd"
                  d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z"
                  clip-rule="evenodd"></path>
          </svg>
        </button>
        <ul id="${collapseId}" class="${childListClasses}">
        `;
        item.Children.forEach(child => {
            html += `<li><hr class="my-1 border-slate-700">${buildMenuItem(child, level + 1)}</li>`;
        });
        html += `</ul>`;
    }




        else if (isClickableLink) {
        html += `
        <a href="${escapeHtml(itemLink)}"
           class="${linkClasses}${activeClass} sidebar-link"
           data-url="${escapeHtml(itemLink)}">
          ${iconHtml}
        <span class="flex-1 whitespace-normal break-words leading-snug">
            ${menuName}
        </span>
            </a>`;
    }

 
            
            else {
               
                html += `
    <div class="${linkClasses}${activeClass}">
      ${iconHtml}
    <span class="flex-1 whitespace-normal break-words leading-snug">
        ${menuName}
    </span>
        </div>`;
            }

            return html;
        }

        
        function removeDuplicateMenuItems(items) {
            const seen = new Set();
            return items.filter(item => {
                if (seen.has(item.MPID)) return false;
                seen.add(item.MPID);
                return true;
            });
        }

        
        function buildMenuItems(items) {
            const rootItems = items
                .filter(x => x.Levels === 1)
                .sort((a, b) => (a.MPSerial || 0) - (b.MPSerial || 0));

            let html = "";
            rootItems.forEach(item => {
                html += `<li><hr class="my-1 border-slate-700">${buildMenuItem(item, 1)}</li>`;
            });

            return html;
        }


            function setActiveSidebar(url) {
        const path = (url || "").split("?")[0].toLowerCase();

        
        document.querySelectorAll("a.sidebar-link").forEach(a => {
            a.classList.remove("bg-slate-700", "text-slate-50");

            const itemUrl = (a.getAttribute("data-url") || "").split("?")[0].toLowerCase();
            if (itemUrl === path) {
                
                a.classList.add("bg-slate-700", "text-slate-50");
            }
        });
    }

             
        function loadPageIntoMainContent(url) {
        const main = document.getElementById("main-content");
        
            if (!main) {
        hideMainLoading();
        window.location.href = url;
        return;
    }


        fetch(url, { headers: { "X-Requested-With": "XMLHttpRequest" } })
            .then(function (response) { return response.text(); })
            .then(function (html) {
                const parser = new DOMParser();
                const doc = parser.parseFromString(html, "text/html");

                const newMain = doc.getElementById("main-content");
                
                    if (!newMain) {
        hideMainLoading();
        window.location.href = url;
        return;
    }

                
                main.innerHTML = newMain.innerHTML;

                
                window.history.pushState({}, "", url);

                
                const titleEl = doc.querySelector("title");
                if (titleEl && titleEl.textContent) {
                    document.title = titleEl.textContent;
                }

                
                setActiveSidebar(url);
                hideMainLoading();
            })
                .catch(function (err) {
        console.error("Error loading page", err);
        hideMainLoading();
        window.location.href = url;
    });

    }




                document.addEventListener("DOMContentLoaded", () => {
        const container = document.getElementById("menu-container");
        if (container && Array.isArray(menuData)) {
            const uniqueMenuData = removeDuplicateMenuItems(menuData);
            container.outerHTML = buildMenuItems(uniqueMenuData);
        } else {
            console.error("Menu container not found or menuData invalid");
        }

       
        setActiveSidebar(window.location.pathname + window.location.search);

        document.body.addEventListener("click", function (e) {
            const link = e.target.closest("a.sidebar-link");
            if (!link) return;

            if (e.button !== 0 || e.ctrlKey || e.shiftKey || e.metaKey || e.altKey) {
                return;
            }

            
                e.preventDefault();

    const url = link.getAttribute("data-url");
    if (!url) return;

    //Overlay
        showMainLoading();
        document.body.style.pointerEvents = "none";

    setTimeout(() => {
        loadPageIntoMainContent(url);
    }, 1000);
    // loadPageIntoMainContent(url);



        });

        window.addEventListener("popstate", function () {
            loadPageIntoMainContent(window.location.pathname + window.location.search);
        });
    });



      
</script>


// NotificationCount
<script>
    document.addEventListener("DOMContentLoaded", () => {

        function setNotificationCount(count) {
            const badge = document.getElementById("notif-count");
            if (!badge) return;

            if (count > 0) {
                badge.textContent = count > 99 ? "99+" : String(count);
                badge.classList.remove("hidden");
            } else {
                badge.classList.add("hidden");
            }
        }

        // مثال للتجربة
        setNotificationCount(1);

    });
</script>



